streamlit>=1.61.1,<2
pandas>=2.1,<3
openpyxl>=3.1,<4
# openai 0.28.0 (legacy ChatCompletion/Embedding.create). Upgrading to openai>=1
# would rewrite every call site in app.py; the speed win is parallel labeling +
# capping refine spirals, not the client. Stay here unless a dedicated migration.
openai==0.28.0
tqdm
scikit-learn>=1.3,<2
unidecode>=1.3,<2
joblib>=1.3,<2
xgboost==2.0.3
requests>=2.31,<3
beautifulsoup4>=4.12,<5
# starlette<0.40.0 removed: it broke Streamlit Cloud with Streamlit 1.61+.
# selenium removed: unused and heavy for Cloud.
